document.addEventListener("DOMContentLoaded", () => {
  const statusLine = document.getElementById("status-line");
  if (statusLine) {
    statusLine.textContent = "买家端骨架已加载，可继续接 Buyer / Trade / Swarm bundle 的正式链路。";
  }
});
